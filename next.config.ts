import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.elsolnec.org"], // Agrega aquí el dominio de la imagen
  },
};

export default nextConfig;
